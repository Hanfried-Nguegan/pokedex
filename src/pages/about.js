import Navbar from "@/components/Navbar";
import React from "react";

const about = () => {
  return (
    <div className="relative bg-pokedex-texture bg-cover bg-no-reapeat bg-center w-full h-screen mx-auto">
      <div className="absolute inset-0 top-[80px]">About/Contact Us </div>
    </div>
  );
};

export default about;
